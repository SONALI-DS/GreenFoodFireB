<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.greenfoodfireb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenFoodFireB">
        <activity android:name=".Sdata.MainSview"></activity>
        <activity android:name=".IntroductoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"/>
        <activity android:name=".SignUpActivity" >

        </activity>
        <activity android:name=".TestAct"/>
        <activity android:name=".qqq" />
        <activity android:name=".EditProfile" />
        <activity android:name=".MainActivity22" />
        <activity android:name=".views.MainActivityVV"/>
        <activity android:name=".MainCategoryActivity" />
        <activity android:name=".Main2Activity"/>



        <activity android:name=".imageslide.MainImageActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>